import React from "react";

import Helmet from "../Helmet";

const SignIn = () => {
  return (
    <Helmet title="Sign In">
      <div className="row sign-in">Sign in</div>
    </Helmet>
  );
};

export default SignIn;
